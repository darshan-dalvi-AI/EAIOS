# Core
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
python-multipart>=0.0.9
httpx>=0.27

# Document parsing
pypdf>=4.2
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1

# Infra clients (optional at runtime — graceful fallbacks built in)
qdrant-client>=1.9
redis>=5.0
psycopg2-binary>=2.9

# Testing
pytest>=8.0

# ── Optional extras (uncomment when needed) ─────────────────────
# mcp>=1.0                       # expose EAIOS as an MCP server (python -m app.mcp_server)
# sentence-transformers>=3.0     # real embeddings (BGE / MiniLM)
# langgraph>=0.2                 # graph-based agent orchestration
# unstructured[all-docs]         # layout-aware parsing
# paddleocr                      # OCR for scanned docs
# celery>=5.4                    # background task queue
# opentelemetry-sdk opentelemetry-exporter-otlp  # mirror traces to OTLP
# langfuse                       # mirror traces to Langfuse
